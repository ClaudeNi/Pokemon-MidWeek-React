import React from "react";
import Map from "../Map/Map";
import "./camera.css";

const Camera = () => {
    return (
        <div className="camera">
            <Map />
        </div>
    );
};

export default Camera;
