import React from "react";
import Player from "../Player/Player";

const GameBoard = () => {
    return (
        <div>
            <Player />
        </div>
    );
};

export default GameBoard;
